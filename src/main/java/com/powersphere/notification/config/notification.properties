# PowerSphere Notification Module Configuration
# ==============================================

# Retry interval in milliseconds for failed notifications (default: 5 minutes)
notification.retry.interval=300000

# Scheduler interval in milliseconds for due notifications (default: 1 minute)
notification.scheduler.interval=60000
